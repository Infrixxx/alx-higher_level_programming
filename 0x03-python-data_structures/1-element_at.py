#!/usr/bin/python3
# 1-element_at.py

def element_at(my_list, idx):
    """retrieves an element from a list"""
    if idx < 0 or idx > (len(mylist)):
        return None
    else
        return (my_list[idx])
